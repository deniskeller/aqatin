import React from 'react';
import { PersonalAccount } from '@view/index';
import { Default } from '@layouts/index';

const PersonalAccountPage = () => {
  return (
    <>
      <Default>
        <PersonalAccount />
      </Default>
    </>
  );
};

export default PersonalAccountPage;
