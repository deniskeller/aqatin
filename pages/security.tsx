import React from 'react';
import { Security } from '@view/index';
import { Default } from '@layouts/index';

const SecurityPage = () => {
  return (
    <>
      <Default>
        <Security />
      </Default>
    </>
  );
};

export default SecurityPage;
